// define the input array
const arr = [1, 2, 3, 4, 5];

// map the original array to a new array with the squares of its elements
const squared = arr.map(x => x * x);

// display the new array
console.log(squared); // [1, 4, 9, 16, 25]